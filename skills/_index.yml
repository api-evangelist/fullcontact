generated: '2026-08-14'
method: generated
source: >-
  Generated by API Evangelist from FullContact's public documentation, the live
  MCP tools/list response, and the artifacts in this repo (conventions/,
  errors/, rate-limits/, authentication/). Every operationId referenced exists
  verbatim in the openapi/ files; every limit, header, error code and billing
  rule is quoted from FullContact's own docs.
description: >-
  Packaged Agent Skills for the FullContact V3 API — short operating
  instructions an agent follows to use the API correctly, including the rules
  that are easy to get wrong: name/location pairing, entitlement-shaped
  responses, the delay-then-429 rate limiter, and the fact that there is no
  idempotency key so every retry re-bills.
skills:
  - file: fullcontact-enrich-a-person.md
    name: Enrich a person with FullContact
    api: openapi/fullcontact-enrich-api-openapi.yml
    operations: [enrichPerson]
    mcp_tools: [fullcontact_enrich_person]
    summary: >-
      Resolve a person from email, phone, social URL, or name + postal address,
      via either the hosted MCP tool or the REST endpoint behind it.
  - file: fullcontact-bulk-enrich-with-webhooks.md
    name: Bulk enrich with FullContact webhook callbacks
    api: openapi/fullcontact-enrich-api-openapi.yml
    operations: [enrichPerson]
    summary: >-
      Enrich a list asynchronously with webhookUrl, correlate callbacks through
      query parameters, and pace against the delay-based rate limiter.
provider_published_skills:
  found: false
  checked:
    - https://docs.fullcontact.com/llms.txt
    - https://github.com/fullcontact
  note: >-
    FullContact publishes no AGENTS.md or skill files. It does publish an
    MCP server, which is the closest first-party agent packaging it ships.
